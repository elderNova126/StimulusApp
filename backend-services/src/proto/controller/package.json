{
  "name": "controller-proto",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "install-dev": "yarn install",
    "prebuild": "rimraf dist codegen && sh tools/generate.sh",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^13.13.52",
    "rimraf": "^3.0.2",
    "typescript": "^3.9.10"
  },
  "license": "MIT",
  "dependencies": {
    "@grpc/grpc-js": "^1.12.6",
    "@types/google-protobuf": "^3.15.12",
    "google-protobuf": "^3.21.4",
    "protobufjs": "^6.11.4"
  }
}
